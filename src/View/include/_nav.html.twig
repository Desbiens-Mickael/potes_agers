<nav class="navbar navbar-expand-lg sticky-top navbar-dark" style="background-color: #0c879a">
  <div class="container-fluid">
    <a class="navbar-brand" href="/"><img alt="Brand" src="/assets/images/logo.png" class="img-responsive" /></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
            <div class="d-flex flex-row justify-content-center align-items-center">
                <a class="nav-link text-light" aria-current="page"
                    href="/category">
                    Je cherche
                </a>
            </div>
        </li>

        <li class="nav-item">
            <div class="d-flex flex-row justify-content-center align-items-center">
                <a class="nav-link text-light" aria-current="page"
                    href={{ session.user ? "/products/add" : "/login" }}>
                    Je partage
                </a>
            </div>
        </li>
      </ul>

      {% if session.user %}
        <div class="d-flex flex-row justify-content-center align-items-center">
            <a class="navbar-nav col-2-ms d-flex flex-row justify-content-center align-items-center" href="/logout">
                <p class="text-light mb-0">Bienvenu {{ session.user.name }}</p>
                <img src="/assets/images/logo_user.png" alt="logo de déconnexion" width="40" height="40">
            </a>
        </div>
      {% else %}
        <div class="d-flex flex-row justify-content-center align-items-center">
            <a class="navbar-nav col-2-ms" href="/login">
                <img src="/assets/images/logo_user.png" alt="logo de connexion" width="40" height="40">
            </a>
        </div>
      {% endif %}


    </div>
  </div>
</nav>
